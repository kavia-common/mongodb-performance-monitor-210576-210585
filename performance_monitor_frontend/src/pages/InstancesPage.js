import React from "react";

// PUBLIC_INTERFACE
export default function InstancesPage() {
  /** Placeholder instances page. */
  return (
    <div className="pm-card">
      <div className="pm-kicker">Instances</div>
      <h1>MongoDB Instances</h1>
      <p>
        This is a placeholder page. In later steps, this will manage connections to multiple MongoDB
        instances (local/Atlas), credentials, and instance-specific configuration.
      </p>
    </div>
  );
}
