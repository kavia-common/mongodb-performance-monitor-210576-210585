import React from "react";

// PUBLIC_INTERFACE
export default function SettingsPage() {
  /** Placeholder settings page. */
  return (
    <div className="pm-card">
      <div className="pm-kicker">Settings</div>
      <h1>Application Settings</h1>
      <p>
        This is a placeholder page. In later steps, this will include polling/WebSocket settings,
        alert defaults, and UI preferences.
      </p>
    </div>
  );
}
